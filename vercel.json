{
  "buildCommand": "pip install -r requirements.txt",
  "devCommand": "python -m uvicorn app.main:app --host 0.0.0.0 --port 5000",
  "installCommand": "pip install -r requirements.txt",
  "framework": "fastapi",
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "/api"
    }
  ]
}
